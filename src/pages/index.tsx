// Login page as a Home page

const Home = () => {

  // interface for email, name, and password
  interface FormProps {
    email: string;
    name: string;
    password: string;
  }
  
  return (
    <div>
      {' ini adalah halaman utama '}
    </div>
  );
};

export default Home;
